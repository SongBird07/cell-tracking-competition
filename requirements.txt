# Python 3.11+ requis (zarr>=3 en depend).
# torch/torchvision NE SONT PAS dans ce fichier -- deja preinstalles sur
# Kaggle/Colab ; a installer separement ailleurs, voir SETUP.md.
#
# Contraintes volontairement minimales (>=) plutot que figees (==) : sur
# Kaggle, numpy/pandas/scipy/scikit-image/matplotlib sont deja installes et
# suffisants pour ce projet -- les figer a une version exacte forcerait pip a
# les mettre a niveau, ce qui casse la compatibilite avec les outils
# preinstalles de l'image Kaggle (bigframes, cudf, numba, etc.) sans aucun
# benefice pour nous.

numpy>=1.24
scipy>=1.10
scikit-image>=0.21
pandas>=2.0
matplotlib>=3.7
networkx>=3.0
tqdm>=4.60

# ceux-la ne sont typiquement PAS preinstalles, donc pas de conflit attendu
zarr>=3
geff>=1.3
laptrack>=0.17
